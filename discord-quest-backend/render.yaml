services:
  - type: web
    name: discord-quest-backend-1
    runtime: python
    runtimeVersion: 3.11.11
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class eventlet -w 1 app:app
